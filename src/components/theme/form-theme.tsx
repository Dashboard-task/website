import React, { useRef, useState } from "react"
import { Alert, Button, Form } from "react-bootstrap"
import { Check } from "react-bootstrap-icons"


/**
 * Composant ajout d'un thème.
 */
export const FormTopicComponent: React.FC = () => {

    return (
        <></>
    )
}

import { Menu } from 'antd';
import React, { Component } from 'react';

type stateContent = {
    title: string,
    content: string,
    priority: string,
    deadline: Date
}

// Tableau des tâches
export class TaskComponent extends Component {
    // state = {
    //     title: null,
    //     content: null,
    //     priority: null,
    //     deadline: null
    // }

    render() {
        return (
            <>

            </>
        )
    }
}